# PRD 검수 보고서 — ANA Geo

**대상:** PRD.md **v1.2** (working tree, 2,019행 — v1.1 대비 +39행)
**검수일:** 2026-08-11 · **라운드:** 3
**판정:** CONDITIONAL PASS

## 판정 근거

critical 0 · major 21 · minor 21 · info 6 (활성 48건)

critical이 0이고 major가 1건 이상이므로 판정 규칙에 따라 **CONDITIONAL PASS**다. 라운드 2와 같은 등급이지만 **major가 31에서 21로 줄었다.**

### 라운드 2 대비

| 구분 | 병합 기준 | 원본 기준 |
|---|---:|---:|
| **해소** | **16** | 20 (구조 10 · ANA 3 · GIS 7) |
| **잔존** | **42** | 45 |
| **신규** | **6** | 10 (구조 7 · ANA 1 · GIS 2) |
| 활성 합계 | **48** | 55 |

*(신규 원본 10건이 병합 6건이 된 것은 STR-050이 GIS-034에, STR-047이 기존 MRG-039에 흡수됐기 때문이다. STR-050은 구조 축이 GIS-034 이관을 받아 발행한 교차 확인 건이다.)*

### 3라운드 추이

| 등급 | R1 | R2 | R3 | R1 대비 |
|---|---:|---:|---:|---:|
| critical | 4 | 0 | **0** | −4 |
| **major** | **38** | **31** | **21** | **−17 (45% 감소)** |
| minor | 18 | 21 | 21 | +3 |
| info | 7 | 6 | 6 | −1 |
| 합계 | 67 | 58 | **48** | −19 |

major가 세 라운드에 걸쳐 38 → 31 → 21로 단조 감소했고, 이번 라운드 감소폭(−10)이 가장 크다. minor가 21에서 정체된 것은 major가 해소되면서 남은 세부 지적이 minor로 강등되거나 새로 드러난 결과다.

### 이번 라운드의 서사 두 가지

**첫째, 결함의 무게중심이 누락형에서 전파형으로 옮겨갔다.** 라운드 1의 major는 "요구가 없다"(FR 부재·매트릭스 미뒷받침)가 대부분이었고 지금은 거의 닫혔다 — 특히 §22 매트릭스는 "범례가 없어 ✓의 뜻을 모른다"(R1) → "범례를 표 자신이 25곳에서 어긴다"(R2) → **"20행 × 7앱의 모든 ✓가 뒷받침 FR 매핑표로 추적된다"(R3)**로 세 라운드에 걸쳐 완결됐다. 반면 이번 신규 major 4건은 전부 **"요구는 있는데 그 요구가 도입한 개념이 문서 전체에 전파되지 않았다"**는 유형이다. 뷰포트를 두 키로 가르고도 그것을 읽는 §24.1·FR-MAP-003·FR-EXP-001·FR-SAT-001을 갱신하지 않은 것(MRG-079), 릴레이를 신설하고도 소속을 정하지 않은 것(MRG-080)이 그렇다. 문서가 성숙하면서 결함의 종류가 바뀌었다.

**둘째, 남은 major의 81%는 세 라운드째 손대지 않은 이월분이다.** 활성 major 21건 중 17건이 이월이고 그중 대부분이 **Top 10 밖이라는 이유로 매 라운드 밀려났다.**

| 이월 묶음 | 건수 | 성격 |
|---|---:|---|
| 구조 축 — v1.0 이후 원문 무변경 | 5 | §11.1 "when applicable", §12 `analysis: null`, §14 전체, FR-SITE-005, §28 "should optionally" |
| GIS 축 — 라운드 1부터 이월 | 10 | Overpass 예산·질의 범위·OSM JSON 변환·Python 매니페스트·travel time 모순·씬 정렬·윈도우 읽기·Turf 거리·좌표 규약·카테고리 레지스트리 |
| ANA 축 — v1.0 이후 원문 무변경 | 2 | §24.3 `where available`, §14 의존성 경계 |
| **소계 (이월)** | **17** | |
| v1.2 신규 | 4 | 전파형 결함 |

개정이 §8·§12·§22·§30에 집중되는 동안 이 영역들은 한 번도 열리지 않았다. **v1.3의 수정 대상은 신규가 아니라 이월분이다** — 신규 4건은 각각 한두 문장으로 닫히지만, 이월 17건은 Top 10 안에 들어오지 않는 한 v1.4에서도 그대로 남는다.

### 검수 범위

세 축 모두 `scope: "full"`이며 미검수 영역은 없다. 축 간 상충 판단도 없었다. 라운드 1·2의 MRG ID를 승계하고 신규만 MRG-078~083으로 덧붙였다.

> **조율 전건 종결.** **병합 결론은 모두 유지됐지만 그중 둘에서 내가 제시한 근거가 정정됐다** — 상세는 각 항목에 적었다.
> 1. **MRG-080** — ANA 축 판정: 릴레이는 앱 산출물이자 별도 프로세스(베이스 근거 3종). 경계는 릴레이 너머의 채널에 있다.
> 2. **MRG-083** — 구조 축이 STR-050을 발행해 교차 확인. **(A)/(B) 택일이 아니라** (A) 정본 + (B) 병존 + 데이터 품질 단서로 확정됐다(GIS 축 실측).
> 3. **MRG-078** — 병합 확정, 정본은 **ANA-014**(STR-044는 포인터). 단 내가 든 근거(last-writer-wins)는 **정합성 축이 철회했다.**
> 4. **MRG-039** — 흡수 확정. 단 내가 적은 "수정 위치 §21 한 곳"은 **틀렸다** — §21과 §26.2 두 곳이다.
> 5. **MRG-034** — GIS 축 자체 정정으로 범위가 폴리곤 후보로 좁혀졌으나 v1.2에서 오히려 날카로워졌다.
>
> 계상 주의: 구조 축 원시 데이터의 활성 29건에는 STR-044 포인터가 포함되므로 **구조 축 고유 활성은 28건**이고, 이 문제는 MRG-078로 한 번만 계상했다.

## 지금 고쳐야 할 것 Top 10

**1~4는 v1.2가 만든 결함이고, 5~10은 세 라운드째 이월분이다.** 이월분을 이번에 처리하지 않으면 v1.4에서도 같은 목록을 보게 된다.

| # | ID | 심각도 | 섹션 | 요약 | 수정 제안 |
|---|----|--------|------|------|-----------|
| 1 | MRG-083 | major | §22 | Site 열 ✓를 제거해 미뒷받침 셀을 해소했으나 §18.4가 필수로 명명한 지표의 공급 경로가 사라졌다 | 취득 FR 신설 + ✓ 복원(**도로는 주요 등급 한정**) + FR-SITE-011 병존 + OSM 주거지 커버리지 단서. MRG-049·MRG-034와 일괄 처리 |
| 2 | MRG-078 | major | §12 | `observedView`가 단수 슬롯이고 `stateVersion` 포함 여부도 미정 | 클라이언트별 `observedViews` 맵 + clientId 발급 규칙, 그리고 별도 엔드포인트로 분리해 §8.2-1의 무예외 규칙 유지 |
| 3 | MRG-079 | major | §24.1 | 뷰포트 이원화가 그것을 읽는 기존 요구들에 전파되지 않았다 | §24.1·FR-MAP-003·FR-EXP-001·FR-SAT-001이 어느 키를 읽는지 각각 명시 |
| 4 | MRG-080 | major | §8.3 | 신설 inbound relay의 소속이 미정이라 §30 항목 12의 판정 대상이 없다 | 릴레이를 §8.1 구조·§30 파일 목록 중 어디에 두는지 확정 |
| 5 | MRG-039 | major | §21, §26.2 | 윈도우 읽기 FR이 없어 §8.4의 Range 전달 요구가 보호할 대상이 없다 | `FR-CD-013 — Windowed Read` 신설 **+ §26.2에 윈도우 읽기 문장 추가** (두 곳) |
| 6 | MRG-035 | major | §16 → §8.4 | Overpass 요청 예산 부재 — 적용 대상이 1개 앱에서 **3개 앱**으로 확대됐다 | 앱별 FR이 아니라 §8.4 공유 계약에 스로틀 규칙을 두는 것이 최소 수정 |
| 7 | MRG-025 | major | §24.3 | 제안→승인이 `where available`로 남아 승인 생략이 문서상 정당화된다 | 구절 삭제 + 즉시 적용/승인 경계 한 문장 (**v1.0 무변경**) |
| 8 | MRG-011 | major | §14 | 의존성 경계 규칙 부재 — server.js에 Express를 얹는 것을 막는 문장이 없다 | 코어는 stdlib + vendored만, npm 런타임 의존성·빌드 단계 없음을 명문화 (**v1.0 무변경**) |
| 9 | MRG-026 | major | §28 | provenance를 필수로 전제하는 절이 다섯인데 §28만 `should optionally` | "shall record"로 교정 (**v1.0 무변경**) |
| 10 | MRG-020 · MRG-021 · MRG-033 | major | §12, §11.1, §18.3 | 나머지 v1.0 무변경 묶음 — analysis 슬롯 배치, property 모델 관계, 가중치 검증 | 세 건 모두 라운드 1 권고가 그대로 유효하다 |

## v1.2가 만든 결함 (신규 6건)

구조 축이 라운드 2의 패턴을 받아 체크리스트에 넣은 **"신설 절 양방향 접합 점검"이 신규 6건 전부를 잡아냈다.** 라운드 2에서 관찰한 패턴을 절차로 만든 것이 그대로 효과를 냈다는 뜻이고, 동시에 **v1.2도 같은 유형을 6건 만들었으므로 이 점검은 개정이 계속되는 한 매 라운드 필수다.**

### 앞선 수정의 부작용 — 1건 (✓ 제거가 요구를 함께 지웠다)

**[MRG-083] major — §22** (GIS-034 + STR-050) `[교차]`
> `Note: the Site column has no external-data ✓ by design — §18.6 keeps external data optional, and candidates/reference features come from loaded GeoJSON or prior apps' exported results.`

v1.2는 §22의 미뒷받침 ✓ 문제를 두 방식으로 처리했다 — FR을 신설하거나(Route·Search·CD), ✓를 제거하거나(Site). **앞의 방식은 잘 작동했지만 Site에 적용한 뒤의 방식이 데이터 경로를 끊었다.** 각주가 제시한 두 대체 경로 중 어느 것도 §18이 스스로 명명한 지표를 공급하지 못한다.

- **"loaded GeoJSON"** — 사용자가 도로망과 주거지 폴리곤을 제품 밖에서 준비해야 한다는 뜻인데, §18 어디에도 요구로 없고 §33 데모의 전제로도 서술되지 않았다.
- **"prior apps' exported results"** — Explorer/Search가 내보낼 수 있는 것은 §16.3 레지스트리의 10종뿐이고 전부 `amenity`류 포인트다. **도로 선형도 주거지 토지이용도 레지스트리에 없다.**

그런데 §18.4는 `roadDistance`·`residentialDistance`를 결과 모델 필드로 두고, §18.2는 hard constraint 예시로 "주거지에서 1km 이상"을, §33은 Site 데모로 "roads, and residential separation"을 지정한다. §18.6은 land use data를 optional·future로 분류한다. **문서가 필수 지표로 제시한 것의 출처가 문서 안에서 optional이거나 아예 없다.**

**구조 축이 이관을 받아 STR-050을 발행하면서 근거 셋을 보탰다.** (1) 주석은 "§18.6 keeps external data optional"을 이유로 들지만 **§18.6의 목록은 NASA POWER·DEM·land use·cadastral·public infrastructure뿐이고 OSM·Overpass는 없다** — §18.6은 도로·주거지 데이터에 대해 아무 말도 하지 않으므로 근거가 되지 못한다. (2) "prior apps' exported results"는 **문서 전체에서 이 주석 한 곳에만 등장한다**(`export`의 유일한 출현). 앱 간 결과 내보내기를 요구하는 FR도 형식도 위치도 없다. (3) §9와의 관계도 불명확하다 — §9는 코드 반입만 금지하므로 데이터 전달 자체는 허용되지만 "다른 앱 디렉터리에 런타임 의존 금지"도 요구한다. Site가 다른 앱 산출물을 런타임에 읽으면 위반이고 사용자가 `data/`로 복사하면 위반이 아닌데 주석은 어느 쪽인지 말하지 않는다. **Site는 후보와 참조 피처 어느 쪽에 대해서도 취득 FR이 없는 유일한 앱이다.**

**수정 — (A)/(B) 택일이 아니다. GIS 축이 데이터 품질까지 측정해 세 항목을 함께 요구하는 것으로 확정했다.** 두 안은 경쟁이 아니라 역할이 다르다 — 둘 다 "어디서 가져오나"에는 답하지만, **"가져온 데이터가 §18.2의 pass/fail 판정을 받칠 만한가"에는 (A)만으로 답이 안 된다.**

**실측 (대전 도심 약 100km² bbox, 2026-08-11)**

| 대상 | 실측 | 판단 |
|---|---|---|
| 주요 도로 `motorway\|trunk\|primary\|secondary` | 1,544 way | §26.1 캡 이내 — (A)로 충분 |
| 전체 `highway=*` | 21,097 way | 캡 10배 초과 — **필터 필수** |
| `landuse=residential` | 303 폴리곤, 합집합 **9.83km² = bbox의 9.8%** | **커버리지 부족** |

인구 약 145만 도시의 실제 주거지 비율은 9.8%보다 훨씬 높다. **OSM 주거지 매핑이 성기다는 뜻이고, 결과적으로 `residentialDistance`가 체계적으로 편향된다** — 매핑되지 않은 주거지 한가운데 있는 후보가 "주거지에서 멀다"로 계산된다. **이 편향이 가장 위험한 자리가 하필 §18.2의 hard constraint("Distance from residential area >= 1 km")다.** pass/fail은 틀려도 사용자가 알아채지 못하는 형태로 나오므로 심각도 사다리의 "틀린 걸 모르고 지나간다" 유형이며, 후보지 선정 앱에서 이 판정이 조용히 틀리면 앱의 존재 이유가 무너진다.

1. **(A) 취득 FR 신설 + §22 ✓ 복원 + 레지스트리 등록** — `major_road: highway~"^(motorway|trunk|primary|secondary)$" (line)`, `residential: landuse=residential (area)`. **도로를 주요 등급으로 한정할 때만 성립한다.** "major road"는 §33의 Route 데모가 이미 쓰는 표현이라 새 개념이 아니다. 레지스트리는 POI 목록이 아니라 **feature class registry(키·태그 셀렉터·기대 지오메트리 3열)로 일반화**해야 한다 — 도로는 선형, 주거지는 면형이라 POI 프리셋 개념에 담기지 않는다. 이는 MRG-049가 요구한 작업과 같으므로 **한 번에 처리한다.**
2. **(B) FR-SITE-011을 폐기하지 말고 병존시킨다** — 역할 태깅된 GeoJSON 입력 경로로, 권위 있는 토지이용 데이터가 있으면 OSM 취득분을 대체한다. §24.4·§28의 provenance를 만족시키려면 역할별 파일에 출처 메타데이터를 요구하고, §33 데모가 실행 가능하려면 §8.1의 `data/`에 샘플을 동봉하고 출처를 README에 적어야 한다(§30 item 5).
3. **데이터 품질 단서** — OSM 유래 주거지 지표를 hard constraint의 단독 근거로 쓰지 않는다. soft criterion으로 두거나, hard로 쓸 경우 §23.3 설명에 커버리지 한계를 노출한다. 문안 예: "Metrics derived from OSM land-use are coverage-limited (measured: ~10% areal coverage in central Daejeon) and shall be labelled as approximations in the score breakdown; a pass/fail constraint relying on them requires an authoritative dataset loaded via FR-SITE-011."

어느 쪽이든 §22 매핑표를 갱신하고, 주석의 §18.6 인용을 실제 범위로 좁히며("§18.6 defers DEM/irradiance-class data"), "exported results" 경로를 유지한다면 §9 관계를 한 문장으로 못박는다 — "Exported results are copied into the consuming app's `data/` directory by the user or by ANA; no app reads another app's directory at runtime (§9)."

> **미측정 영역:** 대안 태그(`building=residential|apartments` 계열)의 커버리지가 더 나은지는 Overpass 응답 실패로 확인하지 못했다. 채택 전 확인이 필요하다.
>
> **함께 처리할 인접 3건 — MRG-083 · MRG-034 · MRG-010.** 구조 축 요청으로 한 묶음으로 둔다. 세 건을 붙이면 **"데이터 취득 → 지표 계산 → 라이브러리 하한"이 한 흐름으로 읽힌다.**
>
> - **MRG-083** (이 항목) — 도로·주거지 피처를 어디서 가져올 것인가.
> - **MRG-034** (Turf 거리 함수) — 가져온 뒤 폴리곤 후보를 어떻게 잴 것인가. GIS 축이 라운드 3에 자체 정정했다: Turf.js 7.3.0에 `pointToPolygonDistance`가 추가되어 점–폴리곤이 네이티브 지원되므로 **잔존 결함은 폴리곤 후보로 좁혀졌다.** 그러나 v1.2에서 이 지적은 오히려 **날카로워졌다** — FR-SITE-006이 "computed with Turf.js"로 라이브러리를 못박으면서 동시에 라인·폴리곤 대상 지표를 명명했기 때문이다. 폴리곤–라인·폴리곤–폴리곤은 여전히 네이티브 지원이 없으므로 대표점 근사 규칙과 §23.3 노출을 어디서 규정할지 정해야 한다.
> - **MRG-010** (§14 의존성 선언) — 그 라이브러리의 버전 하한을 어디에 적을 것인가. v1.2가 FR-SITE-006에 "computed with Turf.js"를 넣으면서 버전 하한 없이 라이브러리를 지목했는데, 위 정정에 따라 **이 하한에 실제로 부하가 걸린다.** 앱별 의존성 선언 절을 신설할 때 §14와 함께 `turf >= 7.3.0`을 명시해야 한다.
>
> **데이터를 얻어도 폴리곤 후보를 어떻게 재는지에서 구현자가 다시 멈추므로**, 셋을 따로 고치면 같은 자리를 세 번 여는 셈이 된다.

**"by design"이라는 표시가 있다는 것과 그 결정의 근거가 성립한다는 것은 다른 문제다.** 주석은 자신을 의도적 설계 결정으로 선언하지만 근거로 든 §18.6이 실제로는 그 결정을 지지하지 않는다 — 이것이 이번 건의 핵심이다.

### ✓ 제거로 해소한 3건 — 구조 축 전수 재검증

같은 함정이 다른 해소 건에도 있는지 물었고, 구조 축이 전수 재검증했다. **판정 기준은 하나다: 제거된 능력을 문서의 다른 곳이 여전히 요구하는가.**

| 해소 건 | 제거된 ✓ | 여전히 요구되는가 | 판정 |
|---|---|---|---|
| STR-002 | Site — OSM·Overpass·POI | **요구됨** — FR-SITE-006 · §18.4 · §33 세 곳 | **문제 이전 → MRG-083** |
| STR-006 | Site — Buffer | 요구되지 않음 — §18 구간 buffer 언급 0건, 하드 제약은 거리 계산만으로 성립 | 해소 유효 |
| STR-039 | CD — Imagery preview | 요구되지 않음 — 썸네일이 목적을 못 채움이 실측 확인, §21.7 evolution path로 이관 | 해소 유효 |

**STR-002의 해소 판정은 되돌리지 않는다.** 그 finding이 기술한 defect(매트릭스 ✓의 FR 미뒷받침 + §18.6 모순)는 실제로 닫혔고, 이전된 문제는 MRG-083으로 별도 추적된다. 되돌리면 하나의 결함이 두 건으로 이중 계상되어 대장이 왜곡되고 severity도 중복 반영된다. 대신 **계보가 보이도록 인접 배치했다** — v1.2는 선택지 (B)를 택했으나 절반만 수행했다. ✓ 제거는 했으나 참조 피처 주입을 §18의 요구로 세우지 않고 §22 주석에 뒀다.

> **일반 원칙 — 매트릭스 셀 제거는 그 능력이 문서 다른 곳에서 요구되지 않음을 확인한 뒤에만 유효한 해소다.** 확인 없이 제거하면 정합성 점검은 통과하면서 요구만 사라진다. 이번 라운드에 셋 중 하나가 그랬다. 이 원칙은 구조 축 체크리스트 4-1에 "제거의 유효성" 항목으로 반영됐다.

### 신설 절 사이의 충돌 — 2건

**[MRG-078] major — §12** (ANA-014 + STR-044) `[교차]`
> `"observedView": null`

두 축이 같은 키의 마감 문제를 반대편에서 들어갔다. **구조 축:** `map.observedView`가 **단수 키 하나**인데 §8.2-2는 "All devices connected to the same app converge automatically"로 다중 디바이스를 명시적으로 전제한다. 두 기기가 붙으면 관측 뷰포트가 같은 슬롯을 덮어써 마지막 쓰기만 남고, ANA의 §24.1 조회는 어느 기기의 것인지 알 수 없다. **ANA 축:** §8.2-1이 "모든 상태 변경은 `stateVersion` +1"이라 하고 §8.2-6은 `resultVersion`까지 명시적으로 포함시키는데 `observedView`를 예외로 두는 문장이 없다. 기본 해석상 포함되며, 그러면 한 기기가 팬할 때마다(300ms 단위로) **다른 모든 기기가 적용하지도 않을 값 때문에 상태를 재요청한다.** 베이스에서 version 카운터의 의미는 "공유 상태가 바뀌었으니 모두 다시 받아라"인데 비공유 값이 그 신호를 올리면 카운터의 의미가 흐려진다.

**수정 — 두 갈래를 함께 적용해야 한다.** (1) **귀속 분리**: `observedView`를 클라이언트별로 둔다(`"observedViews": { "<clientId>": { "center": [...], "zoom": 13, "at": "<ISO8601>" } }`). §8.2에 클라이언트 식별자 발급 규칙을 한 줄 추가하고 §24.1의 조회 대상을 "가장 최근에 갱신된 항목"으로 정의하거나 ANA가 기기를 지목할 수 있게 한다. (2) **카운터 예외**: §8.2-1에 "observedView writes do not increment `stateVersion`"을 명시하거나 별도 엔드포인트(`POST /api/observed-view`)로 분리한다 — 후자가 규칙 수가 줄어 낫다.

**이 지적은 성능 논거가 아니라 계약 정합성 논거로 세워야 한다.** 베이스에서 카운터의 의미는 "공유 상태가 바뀌었으니 모든 접속자가 다시 받아라"인데, 규칙 4가 `observedView`를 "never applied by other clients"인 **비공유** 값으로 정의했으므로 이것이 공유 카운터를 올리면 **카운터가 자기 의미를 잃는다.**

> **정본은 ANA-014이고 STR-044는 포인터다.** 두 축이 서로 정본을 양보해 왕복이 세 번 오갔으나 정합성 축이 정리했다 — 본체가 상태 동기화 계약과 §24.1의 맞물림이라 ANA 축 소관이다. 구조 축의 두 논거와 구체 권고안은 모두 흡수돼 내용 손실이 없다. **구조 축 원시 데이터의 활성 29건에 이 포인터가 포함되므로 구조 축 고유 활성은 28건이며, 이 문제는 여기서 한 번만 계상한다.**
>
> **내가 제시한 병합 근거는 정정됐다.** 나는 "ANA 축 권고 하나(last-writer-wins로 귀속을 못박으면 클라이언트별 슬롯 설계라는 과잉을 막는다)가 두 축의 지적을 함께 닫는다"를 근거로 들었는데, **정합성 축이 그 문장을 철회했다.** last-writer-wins는 카운터 오염만 닫고 **단수 슬롯 문제는 닫지 못한다** — 베이스가 공유 데이터에 LWW를 허용하는 것은 편집 충돌 이야기이고, 여기서 잃는 것은 편집 내용이 아니라 **§24.1 조회의 정확성**이기 때문이다. 병합 결론은 유지되지만 올바른 근거는 "같은 키·같은 문장·같은 수정 지점"이다. 정합성 축도 라운드 1~3 내내 "LWW면 셀프호스팅 단일 사용자에 충분"으로 판단해 왔으나 §8.2-2가 다중 기기를 설계된 시나리오로 못박은 이상 그 전제가 틀렸다고 스스로 정정했다. severity가 minor에서 major로 오른 것도 같은 이유다 — 잃는 것이 편집이 아니라 **조용한 오답**이다.

**[MRG-080] major — §8.3** (STR-046)
> `an **inbound relay** long-polls that inbox and pushes each message into the ANA session`

§8.3이 도입한 inbound relay가 바로 위 "server.js owns these responsibilities" 목록에 없고 §8.1 구조·§30 파일 목록에도 자리가 없어, **앱 산출물인지 ANA 하네스 측 구성요소인지 미정이다.** §30 항목 12가 §8.3 이행을 완료 조건으로 삼으므로 **무엇을 확인해야 하는지 정해지지 않은 채 판정을 요구하는 상태다.**

**ANA 축 판정: 베이스에서 릴레이는 앱 산출물이며 별도 프로세스다.** 근거 셋 — (1) `fakechat-dashboard-agent` SKILL.md의 아키텍처 4요소가 릴레이(`fakechat-bridge.js`)를 대시보드 서버와 나란히 놓인 별개 파일로 규정하고 둘 다 빌딩블록이 앱으로 복사해 넣는 산출물이다, (2) 같은 문서의 구축 절차가 `node fakechat-bridge.js`로 독립 프로세스 기동을 지시한다, (3) 베이스 README가 대시보드와 릴레이를 함께 배포하고 두 프로세스를 같이 띄운다. 따라서 "하네스 측 구성요소이므로 앱 의무는 인박스·피드 API 노출까지"라는 선택지는 베이스와 어긋난다.

**다만 경계는 릴레이가 아니라 그 너머에 있다.** 베이스에서 릴레이는 인박스를 롱폴해 **채널**로 밀고 채널이 세션에 푸시하는데, 채널은 세션 기동 옵션에 붙는 것이라 앱 밖이다. PRD가 §8.3에서 채널 미채택을 선언했으므로 릴레이가 "무엇에" 밀어 넣는지가 앱 안에서 닫히지 않는다.

**수정:** §30 항목 12의 판정 대상을 둘로 가른다 — **앱이 판정받을 것**은 인박스·피드 API 노출, 릴레이 산출물의 동봉과 기동 수단 제공, 응답이 항상 `POST /api/agent`로 오는 경로이고, **앱이 판정받을 수 없는 것**은 세션 측 채널 결합이다. **§9도 함께 봐야 한다** — "릴레이 = 별도 프로세스"로 정하면 §9의 "`node server.js`만으로 실행된다"가 Converse 루프를 완성하지 못한다(베이스가 두 프로세스를 함께 띄우는 이유다). §9에 기동 명령을 보강하거나 릴레이를 `server.js` 안에 흡수해야 하며, 후자는 베이스와 다르지만 **채널을 이미 미채택한 PRD에서는 합리적 단순화가 될 수 있다.** 어느 쪽이든 §8.1 · §9 · §30이 같은 결정을 가리켜야 한다.

### 안쪽 방향 접합 실패 — 2건 (신설 절을 참조해야 할 기존 절이 갱신되지 않음)

**[MRG-079] major — §24.1** (STR-045)
> `- current viewport,`

§12 규칙 4가 뷰포트를 `map.view`/`map.observedView`로 가르면서 "for ANA inspection (§24.1)"이라고 §24.1을 지목했는데, **정작 §24.1은 "current viewport"라는 v1.0 문구 그대로다.** 같은 문제가 FR-MAP-003·FR-EXP-001·FR-SAT-001에도 있다. 두 키의 동기화 의미가 정반대(하나는 전 클라이언트 적용, 하나는 절대 미적용)여서 **어느 것을 읽느냐가 동작을 바꾼다.** 특히 FR-EXP-001·FR-SAT-001은 검색 범위를 결정하므로 결과가 갈린다.
**ANA 축이 눈에 보이는 오작동 사례를 보강했다** — FR-MAP-003(좌표 readout)이 `map.view`를 읽으면 사용자가 팬해도 readout이 갱신되지 않고 ANA가 마지막 설정한 위치에 고정되고, `observedView`를 읽으면 ANA의 "대전으로 이동" 직후 잠깐 어긋난다. **참조 전파 누락이 추상적 지적이 아니라 관측 가능한 결함이라는 뜻이다.**
**수정:** 네 곳이 각각 어느 키를 읽는지 명시한다. §24.1은 조회 목적이므로 `observedView`, 검색 범위를 결정하는 FR-EXP-001·FR-SAT-001은 `view`가 자연스럽다.
*MRG-078과는 별건이다(양축 최종 확정) — 그쪽은 상태 키의 자료구조·동기화 의미론이고 이 건은 참조 전파로 층위가 다르다.*

**[MRG-081] minor — §22** (STR-048)
> 뒷받침 FR 매핑표의 첫 항목이 `§10.1`을 인용

v1.2가 §22 범례를 만족시키려 추가한 매핑표의 첫 항목이 FR이 아닌 §10.1을 인용해, 범례의 "at least one FR **in that app's section**"을 문구상 벗어난다. **같은 유형을 §31에서는 규칙 1 완화로 해소했는데 §22에는 같은 완화가 적용되지 않았다.**
**수정:** §31과 동형의 예외 문구를 §22 범례에도 넣는다 — "cross-cutting requirements of §10.1 may be cited in place of an app-section FR".

### 바깥 방향 접합 실패 — 1건 (신설 절이 참조하는 기존 절이 그 참조를 지원하지 않음)

**[MRG-082] minor — §25** (STR-049 + GIS-035) `[교차]`
> `(§25)` — FR-ROUTE-010의 상한 초과 거부

FR-ROUTE-010은 상한 초과를 §25로 넘기면서 이유를 "so an over-cap request cannot be mistaken for a road disconnection"이라고 명시하는데, **§25의 닫힌 범주 목록 아홉 항목에 대응 항목이 없다**("invalid spatial condition"은 조건식 오류이지 범위 초과가 아니다). 구현자가 이 오류를 기존 항목(가장 그럴듯한 것은 "external API unavailable")으로 표시하면 **FR-ROUTE-010이 막으려던 바로 그 오해가 발생한다.** 같은 이유로 MRG-035가 요청한 rate limit 범주도 여전히 없다.
**수정:** §25에 "analysis area exceeds limit"와 "external API rate limited / quota exceeded" 두 항목을 추가하고, **목록이 닫힌 집합이 아님을 한 줄 밝혀** 향후 같은 누락이 반복되지 않게 한다.

> *STR-047(§8.4가 Range 전달 근거로 §26.2를 인용하나 §26.2에 부분 읽기 개념이 없다)도 같은 바깥 방향 실패이며 MRG-039에 흡수했다(구조 축 동의, severity major). **단 내가 적었던 "수정 위치가 같다"는 틀렸다** — §21.5의 FR-CD-013 신설만으로는 §8.4가 §26.2를 인용하는 공백이 닫히지 않으므로 **§21과 §26.2 두 곳을 함께 고쳐야 한다.**

## 축별 상세 — 잔존 42건

**전체 인용과 근거는 라운드 1·2 산출물(`_workspace/prd-review_prev/`, `_workspace/prd-review_r2/`)과 `04_merged_findings.json`에 보존돼 있다.** 여기서는 v1.2에서 무엇이 달라졌는지를 중심으로 적는다.

### 세 라운드째 이월 — v1.0 이후 원문 무변경 (major 7건)

**이번 라운드에서 가장 실질적인 목록이다.** 아래 일곱 건은 v1.0부터 지금까지 해당 문장이 한 글자도 바뀌지 않았다. 라운드 1의 권고가 그대로 유효하다.

| ID | 섹션 | 무변경 문장 | 라운드 1 권고 |
|---|---|---|---|
| MRG-011 | §14 | 의존성 경계 규칙 자체가 없음 | 코어는 stdlib + vendored만, npm 런타임 의존성·빌드 단계 없음 |
| MRG-020 | §12 | `"analysis": null` | 앱별 모델은 `state.analysis.<key>` 아래로 배치 규칙 명시 |
| MRG-021 | §11.1 | `when applicable` | 분석 결과는 항상 Feature properties에, 앱별 모델은 그 하위 스키마 |
| MRG-025 | §24.3 | `where available` | 구절 삭제 + 즉시 적용/승인 경계 한 문장 |
| MRG-026 | §28 | `should optionally record` | "shall record"로 교정 — 필수 전제 절이 이제 **다섯**이다 |
| MRG-033 | §18.3 | FR-SITE-005 "1.0 or 100%" | 합계 위반 시 동작(거부/재조정) 확정 |
| MRG-010 | §14 | 3개 앱(map·explorer·satellite) 의존성 선언 부재 | 각 앱에 §17.2와 동형의 절 추가. Site는 FR-SITE-006 보강으로 부분 해소됐으나 **FR 본문 안의 언급이지 선언 절이 아니다.** 여기에 `turf >= 7.3.0` 버전 하한을 함께 명시한다 — MRG-034·MRG-083과 한 묶음 |

### GIS 축 이월 major 10건 — 라운드 1부터

Top 10에 들지 않아 세 라운드 연속 손대지 않은 항목이다. v1.2에서 **상태가 달라진 것만** 표시한다.

| ID | 섹션 | v1.2에서의 변화 |
|---|---|---|
| MRG-035 | §16 → §8.4 | **노출 확대** — FR-SEARCH-011로 Search가, MRG-007 해소로 Route가 Overpass 소비자로 편입돼 적용 대상이 3개 앱이 됐다. §17.4 예시 하나가 카테고리 3종 질의를 요구하고 §17.6의 조건 수정이 재취득을 유발하므로 부하는 Explorer보다 나쁠 수 있다. **요구를 §8.4 공유 계약으로 올려야 한다** |
| MRG-039 | §21 | **부분 해소** — 전송 계층은 §8.4의 Range·206 무변형 전달로 해결됐으나 **앱 계층이 잔존한다.** §8.4는 부분 읽기를 *깨지 않을* 것을 요구할 뿐 *하라*고 요구하지 않는다. GIS 축 grep 전수 확인 결과 window·partial read·range 문구가 PRD 전체에서 §8.4 한 곳뿐이라 **전체 장면을 내려받아 crop하는 구현도 §8.4를 위반하지 않는다** — 보존할 range read 자체가 없기 때문이다 |
| MRG-012 | §14 | **필요 지점 증가** — FR-SEARCH-011로 OSM JSON→GeoJSON 변환이 Explorer·Search 두 앱에서 각각 필요해졌다(§9가 import 금지) |
| MRG-036 | §26.1 | **신설 FR이 미정의 용어에 의존** — FR-SEARCH-011의 "within the analysis area"가 Search에는 정의되어 있지 않다(Route는 FR-ROUTE-010이 정의) |
| MRG-014 | §30 | **부분 해소** — 구조 축 STR-043은 §30 requirements.txt·항목 13으로 닫혔고 GIS-007이 잔존한다. §8.5가 "the Python runtime remains an installation prerequisite **declared per §30**"을 추가했다 |
| MRG-016 | §12 | **표면 증가** — map 블록이 `view`/`observedView`로 재구성됐으나 좌표 순서 규약은 여전히 없고 `observedView: null`은 좌표 표현 자체가 미정이다 |
| MRG-049 | §16.3 | **위반 이중화** — `subway_station`이 FR-EXP-008에 더해 신설 FR-SEARCH-011("same registry keys")까지 위반한다 |
| MRG-031 · MRG-038 · MRG-034 | §19.4 · §21.5 · §17.3 | 변화 없음 (travel time 모순 / 씬 정렬 전략 / Turf 폴리곤 거리) |

### ANA 축 잔존 5건

- **MRG-025** (major, §24.3) · **MRG-011** (major, §14) — 위 무변경 표 참조. MRG-011은 §30 item 13으로 라운드 1 권고 (a)만 반영됐고 경계 규칙·vendored 범위·hand-rolled 서버 안전 요구는 미반영이다.
- **MRG-024** (minor, §8.3) — 무변경. 문서 전체에 `uxui-design-system`·`agent-native-app-harness` 언급이 없음을 전문 grep으로 재확인했다.
- **MRG-023** (minor, §10.2) — 무변경. Converse Surface가 여전히 프레임 밖이고 동시 가시성 문구가 없다.
- **MRG-063** (info, §33) — 무변경. **§30 item 11이 생겼으므로 데모에 그 항목을 시연하는 단계를 붙이면 문서 전체가 일관된다.**

### 구조 축 잔존 minor·info

MRG-032(§15.3 ANA 예시 — "Remove all markers." 한 건만 잔존), MRG-042(§14 도입 앱 미기재), MRG-043(§21.3 — 라운드 1 실측으로 방법만 명시하면 되는 상태), MRG-044(§19.5 mode 값), MRG-045(§27 allowlist 강도), MRG-046(§25 일괄 요구 — MRG-082와 함께 처리), MRG-047(§8.1 파일 목록), MRG-048(§1.1 다이어그램), MRG-053(§17.3 절 제목), MRG-072(§17.7 범위 인용 한 줄이 5개 술어의 유일한 판정 근거 — 라운드 2 신규가 그대로 이월), MRG-060 · MRG-061 · MRG-062(info), 그리고 **MRG-067**(§16.3 어휘 정본 — **major→minor 강등**, 아래).

**MRG-067 강등 사유:** 앱 경계 문제가 해소됐다. FR-SEARCH-011이 "using the same registry keys as FR-EXP-008", FR-ROUTE-008이 "(as in FR-EXP-002 / FR-EXP-008)"로 명시해 세 앱이 같은 어휘를 참조한다 — 코드 import가 아닌 어휘 참조라 §9와도 충돌하지 않는다. 잔존은 §17.4의 `subway_station`이 §16.3 프리셋 10종에 없다는 한 건뿐이다.

### GIS 축 잔존 minor·info

MRG-050(§18.2 — MRG-083이 Site 데이터 경로를 정리하면 자연히 닫힌다), **MRG-055**(ODbL·타일 정책 — §10.1 주어가 "Every application"으로 바뀌어 **7개 앱 공통 요구가 됐다, 근거 강화**), MRG-056 · MRG-051 · MRG-057 · MRG-058(변화 없음), MRG-064 · MRG-066(info).

## 교차 확인된 발견

| 병합 ID | 원본 | 대상 | 두 축이 각각 본 것 |
|---|---|---|---|
| MRG-078 | STR-044 + ANA-014 | §12 `observedView` | 구조: 단수 슬롯 vs 다중 디바이스 수렴 전제 / ANA: 비공유 값이 `stateVersion`을 올려 공유 신호를 오염 — 한 조치로 둘 다 닫힌다 |
| MRG-082 | STR-049 + GIS-035 | §25 범주 누락 | 구조: 신설 FR이 인용한 범주가 목록에 없음 / GIS: 기존 범주로 표시하면 FR-ROUTE-010이 막으려던 오해가 그대로 발생 |
| MRG-039 | GIS-011 + STR-047 | §21 윈도우 읽기 | GIS: 앱 계층 요구 부재(grep 전수) / 구조: §8.4의 §26.2 인용이 가리킬 대상 없음 *(흡수 잠정)* |

## 잘된 점

v1.2가 제대로 해낸 부분이다. **다음 개정에서 훼손하지 않아야 한다.**

1. **§22 매트릭스 정합이 세 라운드에 걸쳐 완결됐다.** "범례가 없어 ✓의 뜻을 모른다"(R1) → "범례를 표 자신이 25곳에서 어긴다"(R2) → **행별 뒷받침 FR 목록을 붙여 독자가 직접 검증할 수 있게 만들었다**(R3). §10.1을 "Every application shall support … each implemented in its own code (§9)"로 전 앱 스코프화한 한 문장이 라운드 2 전수 점검의 25셀 중 **14셀을 한 번에 닫았다.**

2. **§8.2-6은 검수가 놓친 구멍을 문서 쪽에서 스스로 닫은 사례다.** "Any change to a layer's `resultVersion` also increments `stateVersion` — otherwise polling clients cannot detect that a result set changed even though the state file did not." §12 Rule 3으로 피처 본문을 분리한 순간 생기는 사각지대를 정확히 짚었다.

3. **수정에 근거를 함께 남기는 관행이 자리잡았다.** FR-ROUTE-010의 거부 조항에 "so an over-cap request cannot be mistaken for a road disconnection", §30 item 13의 Node 버전 근거에 "global fetch is a precondition of §8.4"가 붙었다. **문안만 고친 것이 아니라 왜 그런지가 남아서 다음 수정자가 되돌리기 어렵다.**

4. **문서가 자기 관례를 일관되게 적용하기 시작했다.** §21.7에 §20.5와 같은 형식의 단서("…is therefore an evolution path only, never a v1 acceptance criterion")가 붙었다. 라운드 2에서 "§20.5가 §21.7이 따라야 할 내부 모범 사례"라고 지적한 것이 그대로 반영됐다.

5. **라운드 2 신규 결함이 전부 닫혔다.** ANA 축 2건(인바운드 릴레이·뷰포트 강제 동기화), GIS 축 4건(Imagery preview·상한 초과·Range 전달·FR-SAT-010 세부)이 모두 해소됐고, 특히 2키 의미론은 정합성 축이 설계한 것과 **문장 단위로 일치한다.** 회귀는 없다.

6. **기계 점검이 세 라운드 연속 통과했다.** FR 70개(FR-SEARCH-011·FR-CD-012 신설), 형식·중복·건너뜀 없음, JSON 블록 10개 파싱.

## 재검수 안내

수정 후 "PRD 재검수해줘"로 부분/전체 재검수를 요청할 수 있다. 축별로 좁히려면 "구조만 다시", "정합성만 다시", "실현성만 다시"처럼 지정하면 된다.

라운드 4에서는 이 보고서의 MRG ID로 집계한다. 원본 매핑, 라운드별 상태, 해소 근거는 `_workspace/prd-review/04_merged_findings.json`에 있다.

**v1.3 착수 권고 — 이번에는 순서를 바꾸는 편이 낫다.** 신규 4건(Top 10의 1~4)은 각각 한두 문장으로 닫히므로 빠르게 처리하고, **남은 시간을 이월 17건에 쓰는 것을 권한다.** 세 라운드 동안 신규 결함이 매번 Top 10 상단을 차지하면서 이월분이 계속 밀렸고, 그 결과 v1.0에서 지적된 §11.1·§12 `analysis`·§14·§18.3·§28이 아직 열리지 않았다. 이 다섯은 서로 독립적이라 병렬 수정이 가능하고 라운드 1 권고가 그대로 유효하다.
